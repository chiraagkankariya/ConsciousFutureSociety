# CFS Rebuild — PLAN.md

> Generated by Phase 1 audit. Read this before starting any subsequent phase.

---

## Stack Summary

| Layer | Technology |
|---|---|
| Markup | Vanilla HTML5 |
| Styles | Vanilla CSS (single file: `styles.css`) |
| Animation | GSAP 3.12.5 + ScrollTrigger (CDN) |
| Scripting | Vanilla JS (single file: `script.js`) |
| Dev server | `python3 -m http.server 3000` |
| Dev tooling | Puppeteer (Node, screenshots only — not runtime) |
| Fonts | **None loaded** — no Google Fonts or @font-face in project |
| Bundler | None |
| Framework | None |

---

## File Map

```
/
├── index.html          ← Homepage. Hero markup lines 11–31. HERO FILE.
├── styles.css          ← All styles. Hero styles lines 22–165. Extend only.
├── script.js           ← All JS. Entire file is hero animation. Extend only.
├── assets/
│   ├── being.png       ← DO NOT TOUCH. Master PNG asset.
│   └── space-bg.png    ← Hero background image.
├── hero-animation-v1/  ← DO NOT TOUCH. Snapshot reference only.
├── CLAUDE.md           ← Session rules (updated)
├── BRIEF.md            ← Creative brief
├── TASKS.md            ← Phase task list
├── PLAN.md             ← This file
├── package.json        ← Puppeteer dep only (dev)
└── node_modules/       ← Puppeteer (dev only)
```

---

## Hero Files — DO NOT MODIFY

| File | Protected Zone |
|---|---|
| `index.html` | `<section id="hero">` block (lines 11–31) and `<head>` script tags |
| `script.js` | Entire file — idle float tweens + master ScrollTrigger timeline |
| `styles.css` | Lines 22–185: `#hero`, `#space-bg`, `@keyframes kenburns`, `#space-vignette`, `#ambient-glow`, `#stage`, `#float-anchor`, `.being`, `#original`, `#clone-1` through `#clone-5`, `#light-veil` |
| `assets/being.png` | Absolute — no exceptions ever |
| `assets/space-bg.png` | Do not replace without explicit instruction |

**Hero ScrollTrigger config:** `pin:true`, `scrub:0.8`, `start:"top top"`, `end:"+=5000"`, `anticipatePin:1`

---

## Existing Design Tokens (Current — Pre-Phase 3)

The current `styles.css` uses legacy token names that **do not match** the new `--cfs-*` system in CLAUDE.md:

| Legacy Token | Value | New Token Equivalent |
|---|---|---|
| `--navy` | `#0d1b2a` | → `--cfs-navy-mid` |
| `--navy-deep` | `#080f1c` | → `--cfs-navy` |
| `--navy-lift` | `#132238` | → `--cfs-navy-light` |
| `--gold` | `#c9a84c` | → `--cfs-gold` |
| `--gold-lt` | `#e2c270` | *(no direct equivalent — keep as local alias)* |
| `--gold-dk` | `#9a7c32` | *(no direct equivalent — keep as local alias)* |

**Phase 3 action:** Add all `--cfs-*` tokens to `:root`. Do NOT remove legacy tokens yet — hero styles reference them. After verifying hero is unaffected, legacy tokens can be aliased or left as-is.

---

## Fonts — Current State

- **No fonts loaded.** No `<link>` to Google Fonts, no `@font-face`.
- `#next-section` references `'Cinzel', 'Georgia', serif` as an inline fallback — Cinzel is not loaded so Georgia renders.
- **Phase 3 action:** Load two fonts via Google Fonts `<link>` in `<head>`:
  - Display: **Cormorant Garamond** (elegant serif — premium, spiritual-intellectual feel)
  - Body: **Inter** (clean, readable sans-serif — modern and neutral)
  - Set `--font-display: 'Cormorant Garamond', Georgia, serif`
  - Set `--font-body: 'Inter', system-ui, sans-serif`

---

## Navigation — Current State

- **No navigation exists** in `index.html`.
- **Phase 2 action:** Add `<nav id="cfs-nav">` as the first element inside `<body>`, above `<section id="hero">`. Must be `position: fixed`, `z-index: 100` (above hero's max z-index of 10).
- Nav items: `Home` · `About` · `Initiatives` · `TCFSC 2026` · `Join`

---

## Conference Content — Current State

The local `index.html` contains **zero conference-specific content** — only the hero animation and a placeholder `#next-section` div.

The live site (theconsciousfuturesociety.com) currently shows only: *"The Conscious Future Society Conference | April 25, 2026"* — also minimal.

**Phase 2 implication:** There is no conference content to *migrate* from the local homepage. `tcfsc2026.html` will be built as a structured placeholder page with:
- Event title: "The Conscious Future Society Conference"
- Date: April 25, 2026
- Placeholder sections for: Speakers · Schedule · Registration · Sponsors · Event Details
- All sections clearly marked `[PLACEHOLDER — REPLACE WITH REAL CONTENT]`

---

## Hero Exit → First Section Transition

The hero's `#light-veil` fades to **full white (#ffffff)** at scroll completion. The current `#next-section` uses a `linear-gradient` from white → warm cream → navy. This transition logic is correct and must be **preserved** when `#next-section` is replaced with the real Mission section in Phase 4.

**Phase 4 rule:** The Mission section must begin with `background: #ffffff` or a very light tone at its top edge, then transition to `--cfs-navy`. This creates the seamless hero-to-content handoff.

---

## Proposed File / Component Structure

No new directories are required for this stack. All additions are flat files or appended CSS/JS.

### New files to create:
```
tcfsc2026.html     ← Conference subpage (Phase 2)
```

### Files to extend (never rebuild):
```
index.html         ← Add: <nav>, post-hero sections, <footer> (Phases 2, 4)
styles.css         ← Add: --cfs-* tokens, nav styles, section styles (Phases 3, 4)
script.js          ← Add: section ScrollTrigger animations (Phase 5)
```

### Homepage section order (post-hero, Phase 4):
```html
<nav id="cfs-nav">               <!-- fixed overlay, Phase 2 -->
<section id="hero">              <!-- UNTOUCHED -->
<section id="mission">           <!-- 4.1 -->
<section id="philosophy">        <!-- 4.2 -->
<section id="initiatives">       <!-- 4.3 -->
<section id="vision">            <!-- 4.4 -->
<section id="involvement">       <!-- 4.5 -->
<footer id="cfs-footer">         <!-- Phase 4 -->
```

---

## Risks & Dependencies

| Risk | Severity | Mitigation |
|---|---|---|
| Legacy CSS tokens (`--navy`, `--gold`) used in hero styles | Medium | Add `--cfs-*` tokens alongside legacy; never remove legacy tokens |
| No fonts loaded — adding Google Fonts must not block render | Low | Use `display=swap` param on Google Fonts link |
| Hero `#light-veil` exits to white — first content section must match | Medium | Mission section top edge starts white, fades to navy |
| `#next-section` uses legacy tokens and placeholder font | Low | Replace entirely in Phase 4 — it has no protected content |
| Nav `z-index: 100` must sit above hero `z-index: 10` (light-veil) | Low | Confirmed safe — nav at 100 clears all hero layers |
| GSAP CDN — no SRI integrity attributes | Low | Flag for production hardening; acceptable for dev |
| Phase 2 conference page has no source content to migrate | Low | Build as structured placeholder; clearly label all sections |
| Ken Burns CSS animation runs independently of GSAP | None | Confirmed safe — separate animation context |

---

## Phase Execution Notes

- **Phase 2:** Create `tcfsc2026.html` + add nav to `index.html`. Remove `#next-section` placeholder from `index.html` only after Phase 4 Mission section is confirmed in place.
- **Phase 3:** Add `--cfs-*` tokens + fonts to `styles.css` header block. Do not touch hero rules.
- **Phase 4:** Add sections to `index.html` below hero. Replace `#next-section`. Append new CSS to `styles.css` below existing rules.
- **Phase 5:** Append new GSAP ScrollTrigger section animations to `script.js` below the hero block. Hero block ends at line 122.
- **Phase 6:** QA checklist as specified in TASKS.md.
